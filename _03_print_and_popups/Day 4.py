from tkinter import simpledialog, messagebox, Tk

if __name__ == '__main__':
    window = Tk()
    window.withdraw(

    answer = simpledialog, askstring(title = None, prompt='How old are you')

    age = int(answer)

    age = age + 1